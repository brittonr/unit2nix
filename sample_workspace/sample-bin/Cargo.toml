[package]
name = "sample-bin"
version = "0.1.0"
edition = "2021"

[dependencies]
sample-lib = { path = "../sample-lib", features = ["serde"] }
sample-macro = { path = "../sample-macro" }
sample-build-script = { path = "../sample-build-script" }

[dev-dependencies]
pretty_assertions = "1"
